using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImagepathScript : MonoBehaviour
{
    // Start is called before the first frame update
    public string imagePath="";
    void Start()
    {
        imagePath="";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
